server:
  port: 8501
  tomcat:
    connection-timeout: 20000
    threads:
      max: 50
      min-spare: 5
    max-connections: 2048
    accept-count: 50
    accesslog:
      enabled: false

# 配置数据库连接信息
spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 30MB
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_spzx?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&connectTimeout=3000&socketTimeout=10000
    username: root
    password: root123456
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-timeout: 5000
      validation-timeout: 3000
      pool-name: manager-pool
      leak-detection-threshold: 0
      data-source-properties:
        cachePrepStmts: true
        prepStmtCacheSize: 500
        prepStmtCacheSqlLimit: 2048
        useServerPrepStmts: true
        rewriteBatchedStatements: true
        cacheResultSetMetadata: true
      initialization-fail-timeout: 1
      connection-init-sql: SELECT 1

  # Redis的相关配置 — Lettuce 单连接多路复用，去掉 pool
  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        shutdown-timeout: 100ms
      timeout: 3000ms
      connect-timeout: 2000ms
  mvc:
    servlet:
      load-on-startup: 1
    format:
      date-time: yyyy-MM-dd HH:mm:ss
  main:
    lazy-initialization: true
    allow-bean-definition-overriding: true

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    cache-enabled: true
    default-fetch-size: 100

# MinIO 配置
minio:
  endpoint: http://127.0.0.1:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: spzx-manager

# 微信扫码登录配置
wx:
  login:
    app-id: wxed9954c01bb89b47
    app-secret: a7482517235173ddb4083788de60b90e
    redirect-uri: http://guli.shop/admin/system/index/wxLogin/callback
    auth-url: https://open.weixin.qq.com/connect/qrconnect
    access-token-url: https://api.weixin.qq.com/sns/oauth2/access_token
    user-info-url: https://api.weixin.qq.com/sns/userinfo
    qr-ttl-seconds: 120
    poll-interval-ms: 1500
    mock-mode: true
    mock-base-url: http://localhost:80
